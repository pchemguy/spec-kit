## General

### MVP

I want to build an RPN calculator web app. I am thinking of supporting *add*, *sub*, neg, *mul*, *div*, and *abs* functions. The app should also support clear/reset function, which should also be included in the initialization process.

### Full - No Trig

==``
I want to build an RPN calculator web app and have a packaged desktop version later on. I am thinking of supporting add, sub, neg, mul, div, sqr, sqrt, inv, pow, abs, exp, and ln functions. I also want to have undo capability.
_==

> [!NOTE] Prompt
> 
> I want to build an RPN calculator web app and have a packaged desktop version later on. I am thinking of supporting add, sub, neg, mul, div, sqr, sqrt, inv, pow, abs, exp, and ln functions. I also want to have undo capability.

### Extra (Trig)

I want to build an RPN calculator web app and have a packaged desktop version later on. I am thinking of supporting add, sub, neg, mul, div, sqr, sqrt, inv, pow, abs, exp, ln, and trig functions. I also want to have undo capability.

## Decompose

### MVP

I want to build an RPN calculator web app. I am thinking of supporting *add*, *sub*, neg, *mul*, *div*, and *abs* functions. The app should also support clear/reset function, which should also be included in the initialization process. Help me perform user story decomposition and feature synthesis. 

### Full

I want to build an RPN calculator web app and have a packaged portable desktop version later on. I am thinking of supporting *add*, *sub*, *neg*, *mul*, *div*, *sqr*, *sqrt*, *inv*, *pow*, *abs*, *exp*, and *ln* functions. I also want to have undo capability. The app should also support clear/reset function, which should also be included in the initialization process. Help me perform user story decomposition and feature synthesis. 

### Refine

#### Start

I want to build an RPN calculator web app and have a packaged portable desktop version later on. I am thinking of supporting *add*, *sub*, *neg*, *mul*, *div*, *sqr*, *sqrt*, *inv*, *pow*, *abs*, *exp*, and *ln* functions. I also want to have undo capability. Help me perform user story decomposition and feature synthesis. 

#### Revise

The app should also support clear/reset function, which should also be included in the initialization process. 
